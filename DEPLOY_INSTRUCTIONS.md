# Инструкция по развертыванию на сервер

## ✅ Что уже готово локально:
- ✓ Проект собран в папку `dist`
- ✓ Установлены все зависимости
- ✓ Исправлены конфигурационные файлы
- ✓ `.htaccess` добавлен в папку `dist`

## 📤 Что загрузить на сервер:

### Вариант 1: Через FTP (рекомендуется)

1. **Откройте FTP клиент** (FileZilla, WinSCP, Cyberduck или другой)
   
2. **Подключитесь к хостингу** с помощью:
   - FTP адрес (например: ftp.example.com)
   - Логин
   - Пароль

3. **Перейдите в публичную папку:**
   - Обычно это: `/public_html` или `/www` или `/htdocs`

4. **Включите показ скрытых файлов:**
   - **FileZilla:** View → Show hidden files (или Ctrl+H)
   - **WinSCP:** View → Hidden files

5. **Удалите старые файлы** (если они были на сервере)

6. **Загрузите содержимое папки `dist`:**
   ```
   dist/
   ├── .htaccess  ← СКРЫТЫЙ ФАЙЛ, ВАЖНЫЙ!
   ├── index.html
   ├── assets/
   ├── favicon.ico
   ├── placeholder.svg
   └── robots.txt
   ```
   
   ⚠️ **Загружайте СОДЕРЖИМОЕ папки dist, не саму папку!**

7. **Проверьте что всё загрузилось:**
   - Должны быть видны: `.htaccess`, `index.html`, папки `assets/`
   - `.htaccess` должен быть в корне (вместе с `index.html`)

### Вариант 2: Через SSH/Терминал (если доступно)

```bash
# Подключитесь к серверу
ssh user@example.com

# Перейдите в публичную папку
cd ~/public_html

# Удалите старые файлы
rm -rf *

# Загрузите файлы (если у вас есть прямой доступ к папке dist)
# Или скопируйте файлы через scp с вашего компьютера
```

### Вариант 3: Через cPanel File Manager (если доступно)

1. Откройте cPanel
2. Перейдите в **File Manager**
3. Откройте папку **public_html**
4. Удалите старые файлы
5. Нажмите **Upload** и загрузите файлы из папки `dist/`
6. Убедитесь что `.htaccess` загружен (возможно нужно включить показ скрытых файлов)

## 🔍 Проверка после загрузки:

1. **Откройте сайт в браузере:** https://your-domain.com

2. **Откройте консоль браузера:** F12 → вкладка Console

3. **Если нет красных ошибок** - всё работает! ✅

4. **Если ошибки MIME типа сохранились:**
   - Проверьте что `.htaccess` находится в корне (где `index.html`)
   - Убедитесь что используется Apache сервер (не Nginx)
   - Свяжитесь с хостером для включения `mod_rewrite`

## 📋 Структура папки `dist/` (что должно быть на сервере):

```
public_html/
├── .htaccess                        ← конфигурация сервера
├── index.html                       ← главная страница (1.13 kB)
├── favicon.ico
├── placeholder.svg
├── robots.txt
└── assets/                          ← папка с минифицированными файлами
    ├── index-A_KoPw-H.css          ← стили (62.62 kB)
    ├── index-DodqpK3m.js           ← JavaScript (324.73 kB)
    ├── pitbull-1-B42JD3x5.jpg      ← изображения
    ├── pitbull-2-DXo84Lru.jpg
    ├── pitbull-3-BoEzgHIq.jpg
    └── hero-pitbull-CQp3tr-G.jpg
```

## ⚠️ Важные моменты:

- **`.htaccess` - скрытый файл!** В большинстве FTP клиентов скрывается по умолчанию
- **Работает только на Apache!** Если на сервере Nginx - нужна другая конфигурация
- **Загружайте содержимое `dist`, а не саму папку `dist`**
- **После каждого обновления кода** нужно заново собрать (`npm run build`) и загрузить новую версию `dist`

## 🆘 Если что-то не работает:

1. Проверьте что `.htaccess` находится на сервере (включите показ скрытых файлов в FTP)
2. Убедитесь что загружены все файлы из `dist/` включая папку `assets/`
3. Попробуйте очистить кеш браузера (Ctrl+Shift+Del)
4. Свяжитесь с хостером чтобы узнать:
   - Какой веб-сервер используется? (Apache/Nginx/IIS)
   - Включен ли `mod_rewrite` на Apache?
   - Какая версия PHP?

## 📂 Путь к папке dist:

```
e:\pitbull\pitbull-powerhouse-37-main\pitbull-powerhouse-37-main\dist\
```

Все файлы из этой папки нужно загрузить на сервер в папку `public_html` (или аналогичную)

# .htaccess для /api/ директории
# Убеждаемся что PHP работает и не кешируется

<IfModule mod_rewrite.c>
  RewriteEngine Off
</IfModule>

# PHP выполняется, не перенаправляется
<FilesMatch "\.php$">
  SetHandler application/x-httpd-php
  Header set Cache-Control "no-cache, no-store, must-revalidate"
  Header set Pragma "no-cache"
  Header set Expires 0
</FilesMatch>

# Отключаем листинг директорий
Options -Indexes

# Разрешаем доступ
Require all granted

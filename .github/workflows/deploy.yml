name: Build and Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Stamp version
        run: echo "version=$(git rev-parse --short HEAD)-$(date -u +%Y%m%dT%H%M%SZ)" > dist/version.txt

      - name: Deploy to server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ${{ secrets.FTP_PROTOCOL }} # ftp или sftp
          port: ${{ secrets.FTP_PORT }}
          local-dir: dist/
          # FTP root уже попадает в public_html, а рабочий docroot внутри
          server-dir: public_html/public_html/
          dangerous-clean-slate: true
